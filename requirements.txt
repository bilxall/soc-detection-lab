# SOC Detection & Log Analysis Lab
# Python dependencies — install with: pip install -r requirements.txt

pandas>=2.0.0       # optional: for DataFrame-based log analysis
tabulate>=0.9.0     # pretty-print alert tables in terminal
colorama>=0.4.6     # colored terminal output (Windows compatible)
